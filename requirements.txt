# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36

# Document extraction
pymupdf==1.25.1          # PDF read/extract
python-docx==1.1.2       # DOCX read + code-mode generation
openpyxl==3.1.5          # XLSX read + code-mode generation

# Token counting
tiktoken==0.8.0

# Vector DB + embeddings (local, CPU)
chromadb==1.5.9
sentence-transformers==3.3.1

# File generation
# Two complementary engines:
#   - Rapid mode (Markdown):  pandoc (system pkg) -> docx; weasyprint -> pdf; markdown -> html
#   - Code mode (Python):     reportlab/python-docx/python-pptx/openpyxl run by the agent
markdown==3.7            # Markdown -> HTML (rapid mode)
weasyprint==62.3         # HTML -> PDF (rapid mode, full Unicode + CSS)
pydyf==0.10.0            # weasyprint dep: required pin (0.11+ breaks write_pdf with AttributeError 'transform')
reportlab==4.2.5         # PDF (code mode, precise layout)
python-pptx==1.0.2       # PPTX (code mode)
matplotlib==3.9.2        # charts (code mode: embed into pptx/pdf/docx)
Pillow==10.4.0           # image handling (code mode)

# LLM — LangChain unified layer + Ollama + cloud fallback
langchain-core==1.4.6
langchain-ollama==1.1.0
langchain-anthropic==1.4.6
langchain-openai==1.3.0
langchain-google-genai==4.2.5

# Web search (DuckDuckGo, provider-agnostic, no API key)
ddgs==9.14.4

# Ollama health checks
httpx==0.28.0

# Utilities
pydantic-settings==2.6.1
python-dotenv==1.0.1
